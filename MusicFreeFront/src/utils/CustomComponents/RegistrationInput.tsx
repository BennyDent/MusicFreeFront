import { styled } from "@mui/material";




export const RegistrationInput = styled('input')(({theme})=>({
    marginTop: "5px",
    marginBottom: "5px",
    justifyContent: "center",
   width: "300px"
}))